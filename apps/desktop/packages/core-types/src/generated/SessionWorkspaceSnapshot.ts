// AUTO-GENERATED by @cmux/core-types from the cmux Rust crates (ts-rs).
// Do not edit by hand. Run `bun run generate` to refresh.
import type { SessionCanvasPaneSnapshot } from "./SessionCanvasPaneSnapshot";
import type { SessionGitBranchSnapshot } from "./SessionGitBranchSnapshot";
import type { SessionPanelGitBranchSnapshot } from "./SessionPanelGitBranchSnapshot";
import type { SessionPanelListeningPortsSnapshot } from "./SessionPanelListeningPortsSnapshot";
import type { SessionPanelPinSnapshot } from "./SessionPanelPinSnapshot";
import type { SessionPanelPullRequestSnapshot } from "./SessionPanelPullRequestSnapshot";
import type { SessionPanelRestorableAgentSnapshot } from "./SessionPanelRestorableAgentSnapshot";
import type { SessionPanelShellActivitySnapshot } from "./SessionPanelShellActivitySnapshot";
import type { SessionPanelTerminalStartupSnapshot } from "./SessionPanelTerminalStartupSnapshot";
import type { SessionPanelTitleSnapshot } from "./SessionPanelTitleSnapshot";
import type { SessionPanelTtySnapshot } from "./SessionPanelTtySnapshot";
import type { SessionPanelUnreadSnapshot } from "./SessionPanelUnreadSnapshot";
import type { SessionWorkspaceAgentPidSnapshot } from "./SessionWorkspaceAgentPidSnapshot";
import type { SessionWorkspaceLayoutSnapshot } from "./SessionWorkspaceLayoutSnapshot";
import type { SessionWorkspaceRemoteSnapshot } from "./SessionWorkspaceRemoteSnapshot";
import type { SessionWorkspaceSidebarLogEntrySnapshot } from "./SessionWorkspaceSidebarLogEntrySnapshot";
import type { SessionWorkspaceSidebarMetadataBlockSnapshot } from "./SessionWorkspaceSidebarMetadataBlockSnapshot";
import type { SessionWorkspaceSidebarMetadataSnapshot } from "./SessionWorkspaceSidebarMetadataSnapshot";
import type { SessionWorkspaceSidebarProgressSnapshot } from "./SessionWorkspaceSidebarProgressSnapshot";
import type { SessionWorkspaceSidebarStatusSnapshot } from "./SessionWorkspaceSidebarStatusSnapshot";

export type SessionWorkspaceSnapshot = { workspace_id?: string, process_title: string, custom_title?: string, custom_title_source?: string, custom_description?: string, custom_color?: string, current_directory?: string,
/**
 * Optional command used for the first terminal surface in this workspace.
 * This mirrors macOS `initialTerminalCommand` and is required by
 * restore/fork/external-open flows that launch something more specific than
 * the default interactive shell.
 */
initial_terminal_command?: string,
/**
 * Optional text injected into the first terminal after its PTY opens.
 */
initial_terminal_input?: string,
/**
 * Environment overrides for the initial terminal process.
 */
initial_terminal_environment?: { [key in string]?: string }, layout: SessionWorkspaceLayoutSnapshot | null, layout_mode?: string,
/**
 * The panel id currently zoomed to fill the workspace split area, if any.
 * Omitted when no pane zoom is active.
 */
zoomed_panel_id?: string, panel_titles?: Array<SessionPanelTitleSnapshot>, panel_pins?: Array<SessionPanelPinSnapshot>, panel_unreads?: Array<SessionPanelUnreadSnapshot>, restorable_agent_snapshots?: Array<SessionPanelRestorableAgentSnapshot>,
/**
 * Workspace-level git branch fallback used only when no panel reports a
 * branch. Mirrors canonical `Workspace.gitBranch` as consumed by the
 * sidebar badge projection.
 */
git_branch?: SessionGitBranchSnapshot,
/**
 * Per-panel git branch state keyed by `panel_id`. The ordered panel ids
 * still come from the layout tree, so this field only carries facts.
 */
panel_git_branches?: Array<SessionPanelGitBranchSnapshot>,
/**
 * Per-panel pull request state keyed by `panel_id`. Stale rows are kept so
 * the UI can render canonical secondary-stale badges while the poller
 * refreshes inactive panels.
 */
panel_pull_requests?: Array<SessionPanelPullRequestSnapshot>,
/**
 * Remote workspace connection/proxy metadata. The macOS app owns the live
 * broker; the Windows/Tauri port persists the same control-plane facts so
 * browser panes can be rebound to a published local proxy endpoint.
 */
remote?: SessionWorkspaceRemoteSnapshot,
/**
 * Optional sidebar progress reported by agents/CLI integrations. Mirrors
 * canonical `set_progress`/`clear_progress` workspace metadata.
 */
sidebar_progress?: SessionWorkspaceSidebarProgressSnapshot,
/**
 * Sidebar status pills reported by CLI/agent integrations.
 */
sidebar_status_entries?: Array<SessionWorkspaceSidebarStatusSnapshot>,
/**
 * Rich custom sidebar metadata entries reported by CLI/agent integrations.
 */
sidebar_metadata_entries?: Array<SessionWorkspaceSidebarMetadataSnapshot>,
/**
 * Freeform custom sidebar markdown metadata blocks.
 */
sidebar_metadata_blocks?: Array<SessionWorkspaceSidebarMetadataBlockSnapshot>,
/**
 * Recent sidebar log entries reported by CLI/agent integrations.
 */
sidebar_log_entries?: Array<SessionWorkspaceSidebarLogEntrySnapshot>,
/**
 * Aggregate listening ports for this workspace, sorted and deduplicated.
 * Mirrors canonical `Workspace.listeningPorts`; per-panel contributors
 * live in `panel_listening_ports`.
 */
listening_ports?: Array<number>,
/**
 * Workspace-level listening ports owned by live agent process trees. Kept
 * separate from `panel_listening_ports` so panel recomputes do not erase
 * non-terminal agent facts.
 */
agent_listening_ports?: Array<number>,
/**
 * Workspace-scoped agent root PIDs registered by shell/agent hooks.
 * Scanning their descendant process trees feeds `agent_listening_ports`,
 * which is then rendered by the existing sidebar port badge path.
 */
agent_pids?: Array<SessionWorkspaceAgentPidSnapshot>,
/**
 * Per-panel listening ports keyed by `panel_id`, matching canonical
 * `Workspace.surfaceListeningPorts`.
 */
panel_listening_ports?: Array<SessionPanelListeningPortsSnapshot>,
/**
 * Per-panel terminal TTY names reported by shell/SSH integration. This is
 * used by legacy hook/session mapping and debug terminal compatibility.
 */
panel_ttys?: Array<SessionPanelTtySnapshot>,
/**
 * Per-panel shell activity state as reported by shell integration prompt
 * markers. Mirrors canonical `Workspace.panelShellActivityStates`.
 */
panel_shell_activity?: Array<SessionPanelShellActivitySnapshot>, panel_terminal_startups?: Array<SessionPanelTerminalStartupSnapshot>, canvas_panes?: Array<SessionCanvasPaneSnapshot>, group_id?: string, is_pinned?: boolean, };
