// AUTO-GENERATED by @cmux/core-types from the cmux Rust crates (ts-rs).
// Do not edit by hand. Run `bun run generate` to refresh.
import type { ActivityKind } from "./ActivityKind";
import type { ActivityStatus } from "./ActivityStatus";
import type { AgentSessionRateLimitRow } from "./AgentSessionRateLimitRow";
import type { AgentSessionTheme } from "./AgentSessionTheme";
import type { ProviderId } from "./ProviderId";
import type { ProviderStream } from "./ProviderStream";

/**
 * The push-seam event union.
 *
 * Serializes with a `"type"` discriminator to the exact wire objects in
 * `types.ts`. Optional activity fields (`detail`, `outputDelta`) are omitted
 * when absent rather than serialized as `null`.
 */
export type AgentEvent = { "type": "app.theme", theme: AgentSessionTheme, } | { "type": "app.rateLimitRows", rateLimitRows: Array<AgentSessionRateLimitRow>, } | { "type": "provider.started", sessionId: string, providerId: ProviderId, executablePath: string, arguments: Array<string>, } | { "type": "provider.output", sessionId: string, providerId: ProviderId, stream: ProviderStream, text: string, } | { "type": "provider.activity", sessionId: string, providerId: ProviderId, activityId: string, kind: ActivityKind, status: ActivityStatus, action: string, detail?: string, outputDelta?: string, } | { "type": "provider.turnComplete", sessionId: string, providerId: ProviderId, } | { "type": "provider.exit", sessionId: string, providerId: ProviderId, status: number, };
