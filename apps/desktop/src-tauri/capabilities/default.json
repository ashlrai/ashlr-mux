{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the cmux desktop bootstrap shell.",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:default"]
}

