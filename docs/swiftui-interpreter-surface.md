# Runtime SwiftUI Interpreter: Capability Surface & Roadmap

## 1. Framing: what a tree-walker can reach

The interpreter is a swift-syntax tree-walker that lowers a `.swift` sidebar file into a `RenderNode` IR bridged to native SwiftUI. There is no compiler, no toolchain, and no type checker. The type system is *erased*: correctness is not verified, values carry runtime tags, and dispatch is by tag. This is the decisive simplification. Almost everything SwiftUI exposes is a pure value transform over a child view, and a tree-walker renders pure value transforms natively. What it cannot reach is anything that requires (a) state that survives between renders, (b) a closure re-invoked by the host at a moment the walker did not produce, or (c) child-to-parent value flow.

Four architectural unlocks gate roughly 90% of the remaining surface. They are independent and should land in this order:

1. **A mutable `@State` / `$binding` engine.** Today `evaluate()` rebuilds a fresh read-only `Environment` every call, `Environment` has only `define`/`lookup` with no write-back, and `ButtonAction` is a frozen `[ActionCommand]` of `cmux`/`log`/`openURL`. Replace this with a host-owned mutable state bag keyed by `@State` declaration site (stable across re-walks), a `$value` binding handle (a get/set pair carried as a new `SwiftValue`/`RenderNode` field), and an action executor that runs assignments (`=`, `+=`, `.toggle()`, `.append`) against the bag and schedules a re-walk. This converts the renderer from one-shot to interactive and unblocks every input control.

2. **User struct/enum/custom-View interpretation.** Register `struct`/`enum`/`extension` declarations into a per-type method/computed-property/operator table; add tagged values (`.object(typeName,fields)`, `.enumCase`, `.optional`, `.closure`); bind init args by label. Because types are erased, this needs rich tagged values and tag-keyed dispatch, not a checker. It unlocks custom `View` structs, `@ViewBuilder`, `switch`, `guard`, optionals, and protocol conformances (Identifiable/Equatable/Comparable).

3. **A generic modifier + style registry.** Replace the flat `ModifierArg.value: String` and the Color-only `fill`/`foregroundStyle` path with a structured `StyleValue` (Color | gradient | Material | hierarchical | shadowed) routed through one `resolveShapeStyle`, plus structured geometry values (`Angle`, `UnitPoint`, `CGPoint/Size/Rect`). One resolver turns the whole surface from flat colors into real styling and feeds gradients, trim, rotation, paths, and `.shadow`/`.border`.

4. **Arbitrary-child modifiers.** Give `RenderModifier` an optional child `RenderNode` subtree plus an alignment, and lower a modifier's trailing closure through the existing `evalItems` path (today a modifier's trailing closure is silently dropped and `.background` takes only a color). This single IR change unlocks `.overlay { }`, `.background { }`, `.mask`, `.safeAreaInset`, and `Section` header/footer.

A fifth, smaller unlock — **deferred (fire-time / render-time) closure evaluation** — is needed for the genuinely host-driven cases: `GeometryReader`, `alignmentGuide`, `TimelineView` author body, gesture value payloads, and drop actions. It stores the unevaluated closure plus a parameter name and re-runs `ExpressionEvaluator` when the host supplies the value.

---

## 2. Capability matrix (deduped, grouped by feasibility tier)

Support legend: ● supported · ◐ partial · ○ missing. Priority: P0 highest.

### Tier: leaf (pure RenderNode/bridge additions, no new runtime)

| Symbol(s) | Support | Pri | Mechanism (one line) |
|---|---|---|---|
| `Text(_:)` | ● | P0 | Shipped: `.text` node; string/interpolation flow through `displayString`. |
| `Text` + LocalizedStringKey markdown / `Text(verbatim:)` | ○/◐ | P0/P1 | Add `isMarkdownLiteral` Bool; literal-no-interpolation → `Text(LocalizedStringKey)`, else `Text(verbatim:)`. |
| Text concatenation `+` | ◐ | P0 | Windows/Tauri folds constrained `Text(...) + Text(...)` expressions into one sidebar text node. Native attributed-run modifier preservation and richer text-run IR remain follow-up. |
| `.font` / `.fontWeight` / `.bold` / `.italic` | ◐ | P0 | Windows/Tauri maps common font/text emphasis modifiers to CSS hints, including no-arg active `.bold()` / `.italic()` and explicit bool forms such as `.bold(false)` / `.italic(false)`. Broader font token tables, exact SwiftUI font metrics, and full inherited font behavior remain follow-up. |
| `.monospaced` / `.monospacedDigit` | ◐ | P0 | Windows/Tauri maps `.monospaced()` to a monospace class and `.monospacedDigit()` to a tabular-number CSS hint plus stable class. Native font feature availability and exact SwiftUI font metrics remain follow-up. |
| `.lineLimit` (Int/nil/range/reservesSpace) | ◐ | P0 | Windows/Tauri maps constrained numeric line limits to CSS line clamp and supports `reservesSpace: true` as an approximate min-height hint. Nil/range forms and exact SwiftUI text metrics remain follow-up. |
| `.truncationMode` / `.multilineTextAlignment` / `.textCase` | ◐ | P1 | Windows/Tauri maps constrained truncation (`head`/`middle`/`tail`), alignment (`leading`/`center`/`trailing`), and text-case (`uppercase`/`lowercase`) tokens to stable classes/CSS hints. Exact native truncation algorithms and full token coverage remain follow-up. |
| `.strikethrough` / `.underline` (active/pattern/color) | ◐ | P1 | Windows/Tauri parses constrained active/pattern/color arguments and lowers them to CSS text-decoration hints plus stable classes. Per-decoration style/color when underline and strikethrough are combined remains follow-up. |
| `.tracking` / `.kerning` / `.baselineOffset` | ◐ | P1/P2 | Windows/Tauri maps constrained scalar tracking/kerning to CSS `letter-spacing` and baseline offset to a `vertical-align` hint with stable classes. Exact SwiftUI text-run layout, font metrics, and attributed-run composition remain follow-up. |
| `.fontDesign` / `.fontWidth` | ◐ | P1 | Windows/Tauri preserves constrained font design classes and maps built-in font-width tokens (`compressed`, `condensed`, `standard`, `expanded`) to CSS `font-stretch` hints. Native SwiftUI font metrics, variable font availability, and full `Font.Width` behavior remain follow-up. |
| `.dynamicTypeSize` (token/range) | ◐ | P2 | Windows/Tauri maps common size-category tokens to CSS `font-size` hints with stable classes/data breadcrumbs. Ranges, inherited host/user Dynamic Type propagation, and exact SwiftUI scaling metrics remain follow-up. |
| `Text(_:format:)` / `.formatted` (byteCount, etc.) | ◐ | P1 | Windows/Tauri renders constrained static number styles for `.percent`, `.currency(code:)`, `.notation(.compactName)`, `.byteCount(...)`, simple `Measurement(value:unit:).formatted(.measurement(...))` values, deterministic `Date(timeIntervalSince1970:)` date/time styles, constrained `Text(date, style: .date/.time)`, static `Text(timerInterval: start...end)` snapshots, and static array `.list(type:)` formatting. Relative styles, live self-updating dates/timers, locale negotiation, unit conversion, broad unit catalogs, and full `FormatStyle` composition remain follow-up. |
| `Label(_:systemImage:)` / `.labelStyle` / `.labelsHidden` | ◐ | P0/P1 | Windows/Tauri renders `Label(_:systemImage:)`, constrained `Label(title: { Text(...) }, icon: { Image(systemName:) })`, `labelStyle` metadata, and constrained `.labelsHidden()` breadcrumbs/classes with sidebar-local visually-hidden label chrome for common controls. Arbitrary rich title/icon builders, inherited label style environments, and full platform label-layout semantics remain follow-up. |
| `VStack`/`HStack`/`ZStack(alignment:spacing:)` | ◐ | P0 | Windows/Tauri preserves constrained stack alignment tokens on eager/lazy stacks and lowers them to stable classes plus axis-aware CSS alignment hints. Full SwiftUI alignment guides, baseline alignment, RTL-aware leading/trailing mirroring, and native layout negotiation remain follow-up. |
| `Spacer(minLength:)` / `Divider` | ◐/● | P0 | Windows/Tauri renders `Spacer(minLength:)` with stable data breadcrumbs plus CSS min-size hints, and renders `Divider` as a sidebar-local rule. Native axis-aware layout proposal behavior and exact spacer expansion remain follow-up. |
| `.padding(edges,length)` / `EdgeInsets` | ◐ | P0 | Windows/Tauri supports all-side padding, constrained edge-set forms such as `.padding(.horizontal, 6)` / `.padding([.top, .bottom], 4)`, and `.padding(EdgeInsets(top:leading:bottom:trailing:))` side-specific values. RTL-aware leading/trailing mirroring and native layout negotiation remain follow-up. |
| `.frame(...idealWidth:idealHeight:alignment:)` | ◐ | P0 | Windows/Tauri lowers constrained numeric width/height/min/max/ideal dimensions to safe CSS sizing fields, keeps `maxWidth: .infinity` as the sidebar fill class, and maps constrained `alignment:` tokens to stable classes plus CSS text/flex alignment hints. Full SwiftUI proposed-size/ideal-size negotiation and native wrapper placement remain follow-up. |
| `LazyVStack`/`LazyHStack(alignment:spacing:pinnedViews:)` | ◐ | P0/P1 | Windows/Tauri aliases lazy stacks to the stack renderer while preserving lazy intent, constrained alignment/spacing, and `pinnedViews:` as stable classes plus `data-swift-pinned-views` breadcrumbs. Native virtualization, sticky section headers/footers, and pinned scroll physics remain follow-up. |
| `ScrollView(axes,showsIndicators:)` | ◐ | P0 | Windows/Tauri renders a real `.scrollView` node carrying vertical/horizontal/both axis metadata, `showsIndicators`, stable axis/no-indicator classes, and `data-swift-scroll-*` breadcrumbs. Native paging/snap targets, bounce behavior, scroll physics, scroll-to-position, and exact platform scrollbar semantics remain follow-up. |
| `Grid`/`GridRow` + `.gridCellColumns`/`.gridColumnAlignment`/`.gridCellAnchor` | ◐ | P1/P2 | Windows/Tauri renders `Grid { GridRow { ... } }` with stable grid/grid-row classes, `data-swift-grid-kind` breadcrumbs, and constrained grid-cell column/alignment/anchor modifiers lowered to classes, `data-swift-grid-cell-*`, and CSS hints. Native column measurement, exact spanning/alignment negotiation, and full cell placement semantics remain follow-up. |
| `LazyVGrid`/`LazyHGrid` + `GridItem` | ◐ | P1/P2 | Windows/Tauri preserves `LazyVGrid`/`LazyHGrid` identity, constrained inline/local `[GridItem(.fixed/.flexible/.adaptive)]` summaries, spacing, pinned-view breadcrumbs, and CSS `grid-template-*` hints. Native virtualization, exact `GridItem` sizing/adaptive packing, and custom alignment semantics remain follow-up. |
| `ViewThatFits(in:)` | ◐ | P1 | Windows/Tauri preserves `ViewThatFits` as a semantic sidebar-local group with `in:` axis breadcrumbs/classes and renders interpreted alternatives. Native SwiftUI fit measurement and hiding non-fitting alternatives remain follow-up. |
| `.layoutPriority` / `.fixedSize` / `.hidden` / `.clipped` / `.compositingGroup` | ◐ | P1 | Windows/Tauri maps layout priority, fixed-size, `.hidden()` visibility, clipping, and `.compositingGroup()` to safe CSS hints/classes, including `visibility:hidden` for hidden views and `isolation:isolate` for compositing groups. Native SwiftUI proposed-size negotiation, identity/transition/lifecycle behavior, and rasterized compositing-group semantics remain follow-up. |
| `.safeAreaPadding` / `.contentMargins` | ◐ | P1 | Windows/Tauri parses constrained edge-set, length, `EdgeInsets`, and `contentMargins(..., for:)` placement forms into safe padding hints plus stable `data-swift-safe-area-padding-*` / `data-swift-content-margins-*` breadcrumbs. Native safe-area environment reads, scroll-container margin negotiation, and platform inset behavior remain follow-up. |
| `.containerRelativeFrame(_:)` | ◐ | P1 | Windows/Tauri preserves constrained axis/count/span/spacing/alignment metadata as stable classes, `data-swift-container-relative-frame-*` breadcrumbs, and safe CSS width/flex/min-height hints. Native container proposal math, scroll-target sizing, and exact SwiftUI placement remain follow-up. |
| `Group` / `EmptyView` | ◐/● | P1/P2 | Windows/Tauri renders `Group` as a semantic layout-transparent container and treats `EmptyView()` as a no-op node. Native Group modifier fan-out to each child remains follow-up. |
| `List` / `List(data,id:)` | ◐ | P0 | Windows/Tauri supports trailing-closure `List { ... }` containers and constrained `List(data, id:)` row expansion for sequence expressions already supported by the Swift subset, preserving `id:` as a `data-swift-list-id` breadcrumb. List selection, edit mode, SwiftUI identity diffing, and native platform list chrome remain follow-up. |
| `Section(header:footer:)` / `Section("title")` | ◐ | P0 | Windows/Tauri supports `Section("title") { ... }` and constrained single-view-expression `header:` / `footer:` slots rendered through the safe Swift node path. Arbitrary `@ViewBuilder` header/footer closures and full platform list section chrome remain follow-up. |
| `ForEach(data,id:)` / `ForEach(0..<n, id:\.self)` / index+element | ◐ | P0/P1 | Windows/Tauri expands supported sequence expressions, handles indices/enumerated pairs, local binding collections, and stamps generated top-level rows with `data-swift-id` breadcrumbs for constrained scalar/key-path `id:` values. SwiftUI row identity diffing, move animations, and native reconciliation remain follow-up. |
| `if`/`else` as row generators | ● | P0 | `evalIf` already emits 0/1/N sibling rows. |
| `.swipeActions` / `.refreshable` | ◐ | P1 | Windows/Tauri renders `.refreshable { cmux(...) }` as a sidebar-local Refresh button and `.swipeActions { Button(...) }` as a sidebar-local action tray using the existing safe action path. Native pull-to-refresh physics, platform swipe gestures, edit actions, and full list-row integration remain follow-up. |
| `.listStyle` / `.listRow*` / `.scroll*` token modifiers | ◐ | P1/P2 | Windows/Tauri preserves common list/list-row/style tokens and supports constrained scroll chrome classes/data breadcrumbs for `.scrollContentBackground(.hidden)`, `.scrollIndicators(.hidden, axes:)`, `.scrollClipDisabled()`, `.scrollTargetBehavior(...)`, `.scrollTargetLayout()`, `.scrollBounceBehavior(...)`, `.scrollDisabled(...)`, `.scrollPosition(id:anchor:)`, and `.defaultScrollAnchor(...)`. Native paging/snap targets, bounce physics, scroll-position binding, and exact platform scrollbar semantics remain follow-up. |
| `.badge(...)` | ◐ | P1 | Windows/Tauri preserves constrained scalar/text badge values as stable `data-swift-badge` metadata and renders a sidebar-local pill on the authored node. Native list-row, tab, toolbar, menu, accessibility badge placement, and platform badge aggregation semantics remain follow-up. |
| `.tag` / `.id` | ◐ | P0/P2 | Windows/Tauri preserves constrained `.tag(value)` metadata for picker option rows and uses it for selection matching/write-back. `.id(_:)` is preserved as `data-swift-id` metadata; SwiftUI-style identity/replacement behavior remains state/diff follow-up. |
| Shapes: `Ellipse`, `RoundedRectangle(style:)`, `UnevenRoundedRectangle`, `Capsule(style:)`, `ContainerRelativeShape`, `Path(roundedRect:)`, `Path(ellipseIn:)` | ◐ | P0–P2 | Windows/Tauri supports the common built-in shape tokens, preserves constrained `RoundedRectangle(..., style:)` / `Capsule(style:)` corner-style metadata as classes and `data-swift-shape-style`, approximates `ContainerRelativeShape` as a sidebar-local rounded container, and renders constrained non-closure Path rounded-rect/ellipse convenience initializers as sidebar-local shape approximations with path breadcrumbs and size hints. True continuous/circular native corner geometry, true container-radius reads, imperative `Path { ... }` builders, custom Shape, Canvas, and native vector geometry remain follow-up. |
| `.fill(_:style:)` / `.stroke` / `.strokeBorder` / `.trim` | ◐ | P0/P1 | Windows/Tauri maps constrained fills/strokes to safe color/CSS border hints, preserves `.strokeBorder(...)` as `data-swift-shape-stroke*` breadcrumbs, and renders `.trim(from:to:)` as CSS partial-fill metadata. Full `StyleValue`, `StrokeStyle`, trim-before-stroke vector geometry, and native insettable-shape stroke placement remain follow-up. |
| `LinearGradient` / `RadialGradient` / `AngularGradient` / `Color.gradient` | ◐ | P0–P2 | Windows/Tauri has a safe CSS-backed subset for token color arrays in `.background`/`.foregroundStyle`/`.fill`; full `StyleValue`, stop locations, materials, `UnitPoint`/`Angle` values, and richer blending remain follow-up. |
| Material (`.regularMaterial`/`.bar`/...) | ◐ | P1 | Windows/Tauri maps constrained Material background tokens to safe translucent CSS backgrounds with blur/saturation and stable material classes; native SwiftUI vibrancy/material blending, StyleValue integration, and host-window sampling remain follow-up. |
| Hierarchical styles (`.secondary`/`.tertiary`/...) + multi-arg `foregroundStyle` | ◐ | P0/P1 | Windows/Tauri maps constrained foreground hierarchy tokens (`primary` through `quinary`) to safe colors and stable classes, and widens the named palette (`accent`, `mint`, `indigo`, `brown`). Full inherited `ShapeStyle`, multi-arg/layered foreground styles, and environment propagation remain follow-up. |
| `.clipShape(_:style:)` / `.cornerRadius` | ◐ | P0 | Windows/Tauri maps constrained clip shapes to stable classes and supports `style: FillStyle(eoFill:antialiased:)` metadata as `data-swift-clip-*` breadcrumbs. Exact native clipping paths, fill-rule geometry, and antialias rasterization remain follow-up. |
| `.border` / `.shadow(color:radius:x:y:)` / `.blur` | ◐ | P0/P1 | Windows/Tauri maps constrained scalar/color args to safe CSS border, box-shadow, and filter styles. Native SwiftUI shape-stroke semantics, shadow compositing, and blur material interactions remain follow-up. |
| `.brightness`/`.contrast`/`.saturation`/`.grayscale`/`.hueRotation`/`.blendMode` | ◐ | P2 | Windows/Tauri maps scalar filters plus constrained `.hueRotation(.degrees(...))`, `.hueRotation(.radians(...))`, and `Angle` literals to safe CSS filters, and maps `.blendMode(...)` through a fixed CSS `mix-blend-mode` allow-list. Native SwiftUI compositing groups, color-space behavior, and full blend parity remain follow-up. |
| `.aspectRatio` / `.rotationEffect` / `.scaleEffect` / `.offset` / `.position` / `.zIndex` / `.rotation3DEffect` / `.visualEffect` | ◐ | P1/P2 | Windows/Tauri maps constrained aspect ratio, offset, z-index, scalar rotation/scale, `.position(x:y:)`, and `.rotation3DEffect(.degrees(...), axis: (x:y:z:), anchor:, perspective:)` to safe CSS layout/transform hints, accepts `Angle` literals and known `UnitPoint` anchors for those transform modifiers, and preserves `.visualEffect { ... }` as stable metadata/classes. `anchorZ`, arbitrary UnitPoint geometry, native SwiftUI perspective math, native layout semantics, and GeometryProxy-driven visual-effect transforms remain follow-up. |
| `Angle`/`UnitPoint`/`CGPoint`/`CGSize`/`CGRect` literals | ◐ | P0 | Windows/Tauri now carries constrained structured literals through the Swift expression evaluator: `Angle(degrees:/radians:)`, `Angle.degrees/radians`, known `UnitPoint` tokens plus `UnitPoint(x:y:)`, `CGPoint(x:y:)`, `CGSize(width:height:)`, `CGRect(x:y:width:height:)`, `CGRect(origin:size:)`, `.zero` forms, and member access such as `.width`/`.height`/`.x`/`.degrees`. Full CoreGraphics API coverage, path construction, affine transforms, and host `GeometryProxy` values remain follow-up. |
| Image: `.resizable`, `.renderingMode`, `.interpolation`, `.antialiased`, `.flipsForRightToLeftLayoutDirection`, `capInsets` | ◐ | P0–P2 | Windows/Tauri preserves image presentation modifiers as safe classes and now carries constrained `.resizable(capInsets: EdgeInsets(top:leading:bottom:trailing:), resizingMode:)` metadata plus `.flipsForRightToLeftLayoutDirection(...)` breadcrumbs/CSS mirror hints. Native SwiftUI nine-slice/tile rendering, inherited RTL image behavior, and a full `ImageConfig` bucket consumed before AnyView erasure remain follow-up. |
| `.scaledToFit`/`.scaledToFill`/`.aspectRatio` on images | ◐ | P0/P1 | Windows/Tauri parses `.scaledToFit()` / `.scaledToFill()` as aspect content-mode metadata and renders stable fit/fill classes. Native SwiftUI image layout negotiation, proposal sizing, and object-fit parity remain follow-up. |
| `.symbolRenderingMode`/`.symbolVariant`/`.imageScale`/`.symbolEffect(isActive:)`/`.symbolEffectsRemoved` | ◐ | P1/P2 | Windows/Tauri preserves `Image(systemName:)` name/glyph breadcrumbs, symbol rendering/variant/scale classes, inert `.symbolEffect(...)` breadcrumbs, and `.symbolEffectsRemoved()` reset metadata. Real SF Symbol vector rendering, weights/variable values, and animation effects remain follow-up. |
| Color labeled inits / named statics / `Color("name")` (host resolver) | ◐ | P0 | Extend `colorValue` for colorspace + opacity + asset name; widen `dslColor` palette. |
| `.opacity` (view) / `.opacity` (style transform) | ●/◐ | P0 | View form works; style form needs `StyleValue.opacityWrapped`. |
| `.foregroundColor`/`.foregroundStyle` generalized | ◐ | P0 | Route through `resolveShapeStyle`; accept gradients/Material/hierarchical. |
| `.tint` (real, inherited) | ◐ | P0 | Stop folding tint into `foregroundStyle`; emit real `.tint` so SwiftUI propagates. |
| `.background(_:in:)`/`.background { }`/`.overlay { }`/`.mask`/`.safeAreaInset` | ◐/○ | P0/P1 | **Arbitrary-child modifier** seam + StyleValue + `ShapeSpec`. |
| `.preferredColorScheme` / `.environment(\.key,_)` (write) | ◐ | P2 | Windows/Tauri maps constrained `.preferredColorScheme(.dark|.light)`, `.environment(\.colorScheme, ...)`, and `.environment(\.layoutDirection, ...)` to safe classes, `data-swift-*` breadcrumbs, and CSS `color-scheme`/`direction` hints. Full inherited SwiftUI environment propagation and arbitrary environment keys remain follow-up. |
| Menus: `Menu` (title/label/nested), `.contextMenu`, `Section`, `ControlGroup`, `.menuStyle`, `.controlGroupStyle` | ◐ | P0–P2 | Windows/Tauri supports title-form `Menu`, child-bearing `.contextMenu`, `Section`, sidebar-local `ControlGroup` grouped containers, `.menuStyle` metadata, and constrained `.controlGroupStyle(...)` tokens as classes/data plus sidebar-local chrome hints. Nested/rich menu controls, custom style structs, and full platform menu/control-group semantics remain follow-up. |
| `.keyboardShortcut` / `KeyEquivalent` / `EventModifiers` / `.help` / `.disabled` | ◐ | P0/P1 | Windows/Tauri preserves shortcut keys plus constrained `modifiers:` (`command`, `shift`, `option`, `control`) as `aria-keyshortcuts` and stable classes. Native/global accelerator registration, full `KeyEquivalent` members, and full `EventModifiers` OptionSet semantics remain follow-up. |
| `Button(role:)` / `Button(action:label:)` | ◐/● | P0 | Windows/Tauri preserves constrained `.destructive` / `.cancel` roles on title-form and action/label buttons as stable button classes and dialog action metadata. Full `ButtonRole` surface and platform-native role styling remain follow-up. |
| `LabeledContent` | ◐ | P2 | Windows/Tauri renders constrained `LabeledContent("Title", value:)` and `LabeledContent("Title") { ... }` read-only inspector rows; named-label closures, format-style overloads, and full initializer coverage remain follow-up. |
| `GroupBox` / `.groupBoxStyle` | ◐ | P2 | Windows/Tauri renders constrained `GroupBox("Title") { ... }`, `GroupBox { ... }`, and `GroupBox(label: { ... }) { ... }` as sidebar-local labeled card containers with stable `data-swift-group-box-*` breadcrumbs, and preserves `.groupBoxStyle(...)` tokens as classes/data. Custom `GroupBoxStyle` structs and native platform group-box chrome remain follow-up. |
| `DisclosureGroup` | ◐ | P1/P2 | Windows/Tauri renders constrained `DisclosureGroup("Title") { ... }`, `DisclosureGroup { ... }`, and `DisclosureGroup(isExpanded: $localBool) { ... } label: { ... }` as sidebar-local collapsible sections with stable `data-swift-disclosure-*` breadcrumbs and direct local `@State Bool` expansion write-back. Native `DisclosureGroupStyle`, outline/list integration, and full binding/style semantics remain follow-up. |
| `ContentUnavailableView` | ◐ | P1/P2 | Windows/Tauri renders constrained title/system-image/description forms plus label/description/actions builders as sidebar-local empty-state panels with stable `data-swift-content-unavailable` and SF Symbol breadcrumbs. Native platform styling, search-specific convenience initializers, localization, and full symbol/style semantics remain follow-up. |
| `.searchable(text:prompt:placement:)` | ◐ | P1 | Windows/Tauri renders constrained search fields as sidebar-local chrome with stable `data-swift-search-*` breadcrumbs and direct local `@State String` write-back. Native suggestions, search tokens, scopes, platform search-field placement, and broad `SearchFieldPlacement` semantics remain follow-up. |
| `toolbar` / `ToolbarItem(placement:)` / `navigationTitle` / `.toolbarBackground` / `.toolbarColorScheme` | ◐ | P1 | Windows/Tauri renders static navigation title/subtitle/display-mode chrome, `.toolbar { ToolbarItem(placement:) { ... } }` content with stable toolbar placement classes/data, and constrained toolbar background/color-scheme tokens as sidebar-local nav/toolbar chrome hints plus `data-swift-toolbar-*` breadcrumbs. Native platform toolbar slots, item ordering/collapse, automatic placement rules, color propagation, visibility resolution, and dynamic toolbar customization remain follow-up. |
| `.draggable(_:)` (static payload) | ◐ | P1 | Windows/Tauri maps constrained static string/scalar payloads to browser `draggable`, stable classes, and `data-swift-draggable` breadcrumbs. Native drag sessions, `Transferable`, item providers, preview/content closures, and payload binding remain follow-up. |
| `.contentShape` / `.allowsHitTesting` / `.onTapGesture(count:)` / `.onLongPressGesture` | ◐ | P0 | Windows/Tauri preserves constrained content-shape tokens and `.allowsHitTesting(...)` bool metadata as stable classes/data, maps false hit testing to a safe `pointer-events:none` hint, maps tap-count gestures to button affordances with `data-swift-tap-count`, and maps long-press gestures to hold affordances with `data-swift-long-press-duration-ms`. Native hit-test geometry, gesture composition, recognition priority, keyboard-vs-pointer focus nuance, and full SwiftUI gesture values remain follow-up. |
| Accessibility labels/values/hints/traits/elements/actions | ◐ | P1/P2 | Windows/Tauri maps `.accessibilityLabel`/`.accessibilityHidden`/`.accessibilityValue` to ARIA where possible, preserves `.accessibilityHint`, `.accessibilityAddTraits`, `.accessibilityElement(children:)`, `.accessibilityActivationPoint(...)`, `.accessibilityRepresentation { ... }`, and `.accessibilitySortPriority` as safe render breadcrumbs/classes, renders representation children as a hidden metadata mirror, and supports constrained `.accessibilityAction { cmux(...) }` / `.accessibilityAction(named:) { cmux(...) }` keyboard invocation. Native custom action menus, real activation geometry, native representation substitution, sort behavior, and full VoiceOver semantics remain follow-up. |
| `.redacted(reason:)` / `.privacySensitive()` / `.unredacted()` | ◐ | P1 | Windows/Tauri maps constrained redaction/privacy modifiers to sidebar-local placeholder masking classes plus stable `data-swift-redacted`, `data-swift-redaction-reason`, `data-swift-privacy-sensitive`, and `data-swift-unredacted` breadcrumbs. Native inherited redaction environments, system privacy lock-state propagation, and exact SwiftUI placeholder drawing remain follow-up. |
| `.presentationDetents` / `.presentationDragIndicator` / `.presentationBackground` / `.presentationCornerRadius` | ◐ | P2 | Windows/Tauri hoists constrained presented-content metadata onto sidebar-local panel classes, safe background/radius CSS hints, and `data-swift-presentation-*` breadcrumbs. Native sheet height negotiation, platform drag behavior, material sampling, adaptive presentation behavior, and exact platform geometry remain follow-up. |
| `enumerated()` / `dropFirst`/`suffix`/`dropLast` / richer literals & conversions (`Double()`/`Int()`/`String(format:)`/`min`/`max`/`abs`) | ◐ | P1/P2 | Windows/Tauri supports constrained collection slicing/enumeration helpers, numeric builtins, `Int`/`Double`/`String` casts, common `.formatted(...)` number styles, and a sidebar-local `String(format:)` subset for common `%d`/`%f`/`%@`/`%s` specifiers. Foundation locale behavior, broad printf flags/types, and richer collection derivations remain follow-up. |

### Tier: stateEngine (needs the mutable state bag + binding + action executor + re-walk)

| Symbol(s) | Support | Pri | Mechanism |
|---|---|---|---|
| Re-walk on state change | ○ | P0 | Host-owned observable `StateBag` keyed by `@State` site; writes mark dirty and re-run `evaluate()`. **The gate for this whole tier.** |
| `@State` / `$value` (projection) / `Binding(get:set:)` / `@Binding` param | ◐/○ | P0/P1 | Windows/Tauri seeds local JSON-like `@State` values once per render pass from host state, supports direct `$name` control bindings, and supports constrained `ForEach($localCollection)` element-field paths. Full `Binding(get:set:)`, `@Binding` params, stable declaration-site identity, and broad Swift write-back remain follow-up. |
| Assignment & compound mutation (`=`,`+=`,`.toggle()`,`.append`) | ○ | P0 | Replace `parseAction` 3-case matcher with a statement executor that writes the bag + re-walks. |
| `TextField` / `SecureField` / `TextEditor` | ◐ | P0/P1 | Windows/Tauri consumes direct local `@State` string bindings for editable single-line, secure, and multiline text controls; live/session and constant bindings remain read-only. Single-line Enter and multiline Ctrl/Cmd+Enter can trigger the constrained `.onSubmit` subset. Full `Binding`/focus semantics remain follow-up. |
| `Toggle` / `Slider` | ◐ | P0 | Windows/Tauri consumes direct local `@State` bool/double bindings for editable controls and keeps live/session/constant bindings read-only. Richer Binding semantics remain follow-up. |
| `Picker` + selection matching against `.tag` | ◐ | P0 | Windows/Tauri supports direct local `@State` selection bindings, text/label-like options from static rows or `ForEach`, and constrained `.tag(value)` matching/write-back. Full `.tag` composition, rich option views, custom `Binding`, and menu/form picker semantics remain follow-up. |
| `Stepper` (onIncrement/onDecrement) | ◐ | P1 | Windows/Tauri supports the value-binding form with local `@State` numeric bindings, range bounds, and default step. Custom `onIncrement`/`onDecrement` actions still need the statement executor. |
| `.onSubmit` / `.onChange(of:)` | ◐ | P1 | Windows/Tauri supports a constrained direct-control subset: simple local `@State` assignments plus safe `cmux(...)` actions. Full closure capture, arbitrary statements, and value diffing across re-walks remain follow-up. |
| `DatePicker` / `ColorPicker` | ◐ | P2 | Windows/Tauri supports direct local `@State` string bindings through safe HTML date/time/color controls. Native Swift `Date`/`Color` values, format styles, ranges, labels, and calendar semantics remain follow-up. |
| `ForEach($collection)` (element binding) | ◐ | P1 | Windows/Tauri synthesizes per-index setters for local `@State` arrays of JSON-like values; `$item.field` / `$item[index]` direct-control bindings write back through immutable nested state paths. Full Swift collection identity, custom `Binding`, live/session-data mutation, and broad element-binding composition remain follow-up. |
| `List(selection:)` of `NavigationLink` | ◐ | P0 | Selection cell + route push + lazy detail share one bag. |
| `Form` | ◐ | P2 | Windows/Tauri renders `Form { ... }` as a sidebar-local form/list container with stable classes/data and existing interpreted controls inside. Native platform form row chrome, grouped styling, edit mode, and broader form-specific behaviors remain follow-up. |
| `withAnimation` / `.animation(_:value:)` / `.transition` / `.contentTransition` / `.symbolEffect(value:)` | ◐/○ | P0/P1 | Windows/Tauri preserves inert `.animation`, `.transition`, `.contentTransition`, and `.symbolEffect` modifier metadata as classes and `data-swift-*` breadcrumbs. `withAnimation`, watched-value animation firing, transition diffing, and real symbol-effect runtime behavior still need the state/diff engine. |
| `matchedGeometryEffect` / `@Namespace` | ○ | P1 | Host-owned stable `Namespace.ID` surviving re-renders + state-driven branch swap. |
| `TimelineView` (author body) / `.phaseAnimator` | ◐/○ | P0/P1 | Re-evaluable body thunk + host driver re-binds `ctx`/phase each tick (deferred-closure seam). |
| `.onAppear`/`.onDisappear`/`.task(id:)` as drivers / `.onChange` as driver / `.transaction` | ◐/○ | P1/P2 | Windows/Tauri supports constrained `.onAppear`/`.onDisappear` lifecycle hooks, `.task`/`.task(id:)` mount/id-change hooks, and direct-control `.onChange` handlers for simple local `@State` assignments plus safe `cmux(...)` actions. Exact SwiftUI lifecycle timing, arbitrary closure execution, full async/await task runtime, cancellation/priority/actor semantics, true value-diffing, and `.transaction` remain follow-up. |
| Gestures/focus: `.onHover`, `.hoverEffect`, `.defaultHoverEffect`, `.focusable`, `.focused`, `DragGesture(onChanged/onEnded)`, `.gesture`/`.simultaneous`/`.highPriority`, drop highlight, focus actions, gesture `.value` exposure | ◐/○ | P0–P2 | Windows/Tauri supports constrained `.onHover { hovering in ... }` mouse enter/leave local handlers, `.hoverEffect(...)` / `.defaultHoverEffect(...)` pointer-hover tokens as stable classes/data plus sidebar-local hover chrome hints, `.focusable()` keyboard focus metadata, and `.focused($localBool)` browser focus/blur mirroring for direct local `@State` Bool bindings. Drag gestures, composed gestures, gesture value payloads, focus actions/scopes, drop highlight, native pointer-region hover animation, inherited default hover environments, and native focus propagation semantics remain deferred fire-time/runtime follow-up. |
| `.dropDestination(for:action:)` | ◐ | P1 | Windows/Tauri supports a constrained command target: `.dropDestination(for:) { cmux(...) }` marks a rendered node as a browser drop target and invokes the precomputed safe action on drop. Deferred payload/location binding, typed `Transferable` values, native drop highlighting, and full drop result semantics remain follow-up. |
| Menus: `Toggle`/`Picker` in menus, `contextMenu(forSelectionType:)` | ○ | P1/P2 | Two-way binding + `.tag` matching. |
| Navigation: `NavigationStack(path:)`, `NavigationLink(value:/destination:)`, `navigationDestination`, `NavigationSplitView`, `dismiss`, `TabView(selection:)` | ◐ | P0/P1 | Windows/Tauri supports a sidebar-local `NavigationStack` plus static-destination `NavigationLink("Title") { ... }` / `NavigationLink(destination:) { ... }` push stack, a constrained string-like `NavigationLink(value:)` + `.navigationDestination(for:)` route subset, and non-selection `TabView` containers with `.tabViewStyle(.page)` metadata/scroll-snap hints plus simple `.tabItem { Text/Label }` chrome. `path:` bindings, broader route/value matching, split navigation, dismiss, tab selection, native page indicators, and exact platform tab/paging behavior remain follow-up. |
| `Link` / `OpenURLAction` | ◐ | P1 | Windows/Tauri renders constrained `Link("Title", destination: URL(string: "https://...")!)` and `Link(destination:) { ... }` label builders as sidebar-local external anchors for safe `http`/`https` URLs with stable `data-swift-link-*` breadcrumbs. Unsafe schemes render as inert disabled links. Native `OpenURLAction`, custom scheme dispatch, environment override behavior, and platform URL policy remain follow-up. |
| Presentation: `sheet(isPresented:/item:)`, `popover`, `fullScreenCover`, `alert`, `confirmationDialog` | ◐ | P0–P2 | Windows/Tauri supports sidebar-local panels for simple local `@State Bool` `isPresented:` bindings and a constrained optional/string-like `item:` subset for sheet/popover/fullScreenCover/alert/confirmationDialog; close flips Bool cells false or local item cells null, `dismiss()` button/action closures inside presentation content close the active panel, presented-content detent/drag-indicator metadata becomes panel chrome breadcrumbs, and direct alert/confirmation child `Button(role:)` nodes render as action rows. Native modal/window semantics, full environment `DismissAction`, native detent height negotiation, Identifiable item semantics, and full lazy host presentation remain follow-up. |
| `class` / `mutating func` / `@Bindable` | ○ | P2 | Reference-identity cells in the bag; mutation visible through aliases. |
| `let`/`var` reassignment, while/repeat loops, custom operator `+=` write-back | ◐/○ | P0–P2 | Reassignment needs mutable scope cells; loops need a bounded cap + control-flow signal. |

### Tier: typeSystem (needs declaration registry + tagged values + control-flow signal)

| Symbol(s) | Support | Pri | Mechanism |
|---|---|---|---|
| `struct` definition + memberwise init | ○ | P0 | Register `StructDeclSyntax`; tag `.object(typeName,fields)`; synthesize label-matched init. |
| custom `View` struct + `var body: some View` | ○ | P0 | Bind init args by label into scope + self; `evalItems(body)`. |
| `@ViewBuilder` (param + func) | ○ | P0 | Store as `.closure(params,body,capturedScope)`; expand `content` later via `evalItems`. |
| `enum` (raw + associated values) | ○ | P0 | `.enumCase(typeName,case,payload)`; `.rawValue`/`init?(rawValue:)`. |
| `switch`/`case` (+where, value/enum/tuple/range patterns, let-binding) | ○ | P0 | Pattern matcher in `evalItems` (view) and `evalBlockValue` (value); bind let names in child scope. |
| `if let` / `guard let` / `?.` / `??` | ○ | P0 | `SwiftValue.optional`; `evalIf` reads `OptionalBindingCondition`; member/subscript propagate `nil`. |
| `guard` statement | ○ | P0 | Control-flow signal: else block must emit `.return/.break/.continue`. |
| computed property `var x: T { ... }` | ○ | P0/P1 | Detect getter accessor; evaluate block with self-members bound. |
| `@ViewBuilder` helper func + labels/defaults/variadics/tuple-return | ◐ | P0/P1 | Core path exists; add label-matched binding, defaults, tuple returns. |
| `extension` (methods/computed props on existing/built-in type) | ○ | P1 | Merge members into the per-type table; dispatch by runtime tag/kind. |
| protocols (Identifiable/Equatable/Comparable/Hashable) | ○ | P1 | Identity by `.id` member; `==` structural; `<` routes to declared `static func <`. |
| generics (erased) | ○ | P1 | Parse and ignore constraints; a generic container is a struct with a closure field. |
| `break`/`continue`/`return` | ◐ | P1 | Replace bare-value block evaluator with a `ControlFlow` signal enum. The keystone refactor for guard/loops/early-return. |
| `do`/`try`/`catch` + throwing funcs / `try?` | ○ | P2 | `.throw(value)` signal unwinds to nearest `do/catch`; `try?` → `.optional(nil)`. |
| `KeyPath` literal `\.member` | ◐ | P0 | Constrained collection projection: `map(\.x)`, `compactMap(\.x)`, `flatMap(\.x)`, `sorted(by: \.x)`, plus `ForEach(..., id: \.self)` metadata; no first-class `.keyPath` value or grouping yet. |
| `@Observable` model + `@State` ownership / static members | ○ | P1/P2 | Register class def; seed mutable `.object`; `$m.field` projects a binding keypath. |
| `Dictionary(grouping:by:)` / collection derivations (`min`/`max(by:)`/`allSatisfy`/keyPath-`sorted`) | ◐ | P1 | Windows/Tauri supports constrained `Dictionary(grouping:by:)` records plus array `.min(by:)`, `.max(by:)`, `.allSatisfy`, and key-path sort/projection forms. Full typed `Dictionary<Key,Value>`, grouping semantics, optional chaining, and protocol-driven comparison remain follow-up. |
| `AsyncImage(content:placeholder:)` (closure) / `Label(title:icon:)` (closure) | ◐ | P1 | Windows/Tauri supports `Label(title:icon:)` plus constrained named and labeled trailing `AsyncImage(url:content:placeholder:)` closures, including `image in image.resizable()` parameter bodies. Arbitrary `Image` value passing, phase closures, and native load lifecycle remain follow-up. |
| `AsyncImage` (phase form) | ○ | P2 | Needs `switch` over phase enum + associated-value binding. |
| `@Environment(\.key)` reads / `.environment` write-back-to-author | ◐ | P1/P2 | Windows/Tauri parses constrained read-only `@Environment(\.colorScheme)`, `@Environment(\.layoutDirection)`, and `@Environment(\.locale)` declarations with stable sidebar defaults usable in text, branches, and actions. Host/user preference propagation, arbitrary keys, write-back-to-author inheritance, and `dismiss`/scene values remain follow-up. |

### Tier: runtimeLimited (works only because the host/SwiftUI owns the capability)

| Symbol(s) | Support | Pri | Mechanism |
|---|---|---|---|
| `Image("name")` / `Image(decorative:)` (asset catalog) | ◐ | P1/P2 | Windows/Tauri resolves names through the host-provided `extension.sidebar.snapshot.assets` map and renders safe `http`/`https` or `cmux-sidebar-asset://` URLs; missing/unsafe assets become placeholders. `Image(decorative:)` forces empty alt text plus `aria-hidden` so authored labels do not leak onto decorative assets. Native asset catalog lookup remains host follow-up. |
| `AsyncImage(url:)` (load lifecycle) | ◐ | P1 | Windows/Tauri lowers safe `http`/`https` URL leaves to bounded lazy remote `<img>` nodes with constrained `data-swift-async-image-phase` / safe-url breadcrumbs; unsafe schemes become failure placeholders. Named content/placeholder closures can render interpreted success/placeholder children, but phase closures and true load lifecycle remain type-system/runtime follow-up. |
| `@FocusState` + full `.focused` | ◐/○ | P2 | Windows/Tauri has a constrained `.focused($localBool)` subset that mirrors browser focus/blur into direct local `@State` Bool bindings on focusable nodes. Full `@FocusState`, programmatic focus ownership, focus scopes, and native focus propagation still need the bridge-owned focus cell/runtime seam. |
| `alignmentGuide(computeValue:)` | ◐ | P2 | Windows/Tauri preserves constrained alignment-guide tokens plus static numeric closure offsets as stable classes, `data-swift-alignment-guide*` breadcrumbs, and safe CSS margin hints. Native `ViewDimensions` closure evaluation and SwiftUI alignment negotiation remain deferred host-borrowed follow-up. |
| `GeometryReader` / `.coordinateSpace` | ◐/○ | P2 | Windows/Tauri preserves constrained `.coordinateSpace(name:)` / `.coordinateSpace(.named(...))` registration metadata as stable classes and `data-swift-coordinate-space`. `GeometryReader`, host `GeometryProxy`, frame conversion, and named-space lookups remain deferred host-borrowed follow-up. |
| `Text(_:style:)` / `Text(timerInterval:)` (self-updating) | ◐ | P1/P2 | Windows/Tauri renders constrained static `Text(date, style: .date/.time)` output and `Text(timerInterval: start...end)` duration snapshots with stable `data-swift-text-style` / timer breadcrumbs. Native self-updating runloop animation, pause time, relative date ticking, and richer date style semantics remain follow-up. |
| `.scrollPosition(id:)` / `ScrollViewReader.scrollTo` | ◐/○ | P2 | Windows/Tauri preserves constrained `.scrollPosition(id:anchor:)` and `.defaultScrollAnchor(...)` metadata as stable classes/data breadcrumbs. Real binding-driven position sync, host-pushed scroll intents, `ScrollViewReader` proxy objects, and `scrollTo` remain follow-up. |

### Tier: infeasible (cannot be expressed in a single-pass view-only tree-walker)

| Symbol(s) | Why |
|---|---|
| `PreferenceKey` + `.preference` / `onPreferenceChange` | Child-to-parent aggregation with a user `reduce`; contradicts one-pass top-down flow and needs protocol conformance. |
| `commands` / `CommandMenu` / `CommandGroup` | Attach to a `Scene`/`WindowGroup`; the interpreted sidebar is a view embedded in the host window with no author-controllable Scene. Route global shortcuts through `KeyboardShortcutSettings`. |
| `Canvas`, `Path { p in … }` builder, custom `struct: Shape` | Need a stateful imperative builder/context object plus (for custom Shape) struct+protocol conformance and a render-time rect; the union of every hard part. Windows/Tauri covers constrained non-closure `Path(roundedRect:)`/`Path(ellipseIn:)` convenience initializers only. |
| `.keyframeAnimator` general form | Needs a user value struct + `KeyframeTrack(\.keypath)` keypaths. A hardcoded named-track subset (scale/opacity/offset/angle) is `typeSystem`-feasible; `phaseAnimator` covers most needs cheaply. |
| Custom `HorizontalAlignment`/`VerticalAlignment` IDs | Need `extension { static let }` + `AlignmentID` conformance synthesis. |
| Swift Charts, arbitrary `AttributedString` value building | Out of vocabulary; `AttributedString(markdown:)` via a recognized nested-call shape is a leaf, general construction is not. |

---

## 3. Phased roadmap

### Phase 1 — leaf wins (pure IR + bridge, no new runtime)

**Interpreter changes.** Add the structured value substrate to `SwiftValue` (`Angle`, `UnitPoint`, `CGPoint/Size/Rect`, `Date`). Replace `ModifierArg.value: String` with a structured `StyleValue` and add one shared `resolveShapeStyle`. Add a typed payload channel on `RenderNode` for non-string values (`[GridItem]`, axis sets, edge sets). Add the arbitrary-child modifier seam (`RenderModifier.content: [RenderNode]` + `alignment`) and lower modifier trailing closures via `evalItems`. Add an `ImageConfig` bucket consumed on the concrete `Image` before AnyView erasure. Add the new view kinds and thread the already-dropped stack alignment args.

**Unlocks.** Real layout (alignment on every stack, edge-set padding, ideal frame); `List`/`Section`/`LazyVStack`/`LazyHStack`/`ScrollView(axes)`/`Grid`/`LazyVGrid`/`ViewThatFits`/`Group`/`EmptyView`; all text typography (markdown discriminator, `Label`, text concatenation, italic/monospaced/tracking/case/design, line-limit ranges); full styling (gradients, Material, hierarchical styles, `.fill`/`.stroke`/`.strokeBorder`/`.trim`, `.clipShape`/`.clipped`/`.mask`, `.border`/`.shadow`/`.blur`, transforms, `.overlay { }`/`.background { }`/`.safeAreaInset`); image config (`.resizable`/`.scaledToFit`/`.aspectRatio`/`.renderingMode`, SF Symbol expressiveness); menus (`Menu`/`.contextMenu`/`Section`/`ControlGroup`/`.keyboardShortcut`/`.help`/`.disabled`/`Button(role:)`); static `toolbar`/`navigationTitle`; `.draggable` (static), `.onLongPressGesture`, `.contentShape`, tap count; richer value methods (`enumerated`/`dropFirst`/conversions). Self-updating `Text(_:style:)`/`Text(timerInterval:)` land here too (host-driven, no `@State`).

### Phase 2 — state engine (`@State`/`@Binding`/`$` + input controls)

**Interpreter changes.** Lift a host-owned observable `StateBag` (`[siteKey: SwiftValue]`) into the model, seeded once from `@State` decls and surviving re-walks. Add a binding handle (`SwiftValue.binding(get,set)` produced by `$name`/`$obj.field`/`$arr[i]`). Replace `parseAction`'s three-call matcher with a statement executor (`=`,`+=`,`-=`,`.toggle()`,`.append`, multi-statement) that writes the bag and schedules a re-walk; keep `cmux`/`log`/`openURL` as terminal side effects. Add value-diffing across re-walks (for `onChange`) and stable identity stamping on `RenderNode` (for transitions/diffing). Add a deferred fire-time action variant (store unevaluated arg exprs + callback param name) for gesture/drop payloads.

**Unlocks.** `TextField`/`SecureField`/`TextEditor`/`Toggle`/`Picker`(+`.tag`)/`Slider`/`Stepper`/`DatePicker`/`ColorPicker`; `ForEach($collection)`; `.onSubmit`/`.onChange`; `withAnimation`/`.animation(value:)`/`.transition`/`.contentTransition`/`.symbolEffect(value:)`/`matchedGeometryEffect`/`@Namespace`; author-controllable `TimelineView`/`.phaseAnimator`; live gestures (`onHover`, `DragGesture`, drop highlight, gesture `.value`); `.onAppear`/`.onDisappear`/`.task(id:)` drivers. Windows/Tauri has constrained `.onHover`, `.focusable`, and `.focused($localBool)` subsets already; richer gesture/focus payloads remain in this phase. `@FocusState` rides here but stays runtime-limited (bridge owns the real focus state).

### Phase 3 — type-system layer (user types + control flow)

**Interpreter changes.** Add the `ControlFlow` signal enum (`.normal/.return/.break/.continue/.throw`) replacing the bare-value block evaluator — the keystone refactor. Add tagged values (`.object(typeName,fields)`, `.enumCase`, `.optional`, `.closure`, `.keyPath`). Walk `struct`/`enum`/`extension`/computed-property/`@Observable` declarations into a per-type method/computed-prop/operator table dispatched by runtime tag. Synthesize label-matched memberwise inits; bind init args by label. Add `switch`/`guard`/optional-binding handling and `?.`/`??`.

**Unlocks.** User `struct`/`enum`, custom `View` structs + `var body`, `@ViewBuilder` content composition, `switch`/`case`, `guard`, `if let`/`guard let`, optionals/optional-chaining, computed properties, labeled/default/variadic func params, `extension`, protocol conformances (Identifiable/Equatable/Comparable), erased generics, `KeyPath`-driven `id:`/sort/group, `AsyncImage`/`Label` closure forms, `@Environment` reads. `do/try/catch` and the constrained `keyframeAnimator` subset are the tail of this phase.

### Phase 4 — runtime-limited (navigation / presentation / async / deferred-layout)

**Interpreter changes.** Add an internal navigation-command family (`navigate.push/pop/present/dismiss`) the host resolves against path/selection/presentation cells (distinct from host-opaque `cmux()`), plus an interaction-driven re-walk that lazily evaluates destination/sheet/popover/tab bodies only when presented, binding the route/item into a child scope. Add the deferred render-time closure seam: a callable `SwiftValue` the bridge re-invokes inside `GeometryReader`/`alignmentGuide` with host-provided `GeometryProxy`/`ViewDimensions`. Add the host asset resolver and let SwiftUI own async image loading.

**Unlocks.** `NavigationStack`/`NavigationLink`/`navigationDestination`/`NavigationSplitView`/`dismiss`/`TabView`; `sheet`/`popover`/`fullScreenCover`/`alert`/`confirmationDialog`; `GeometryReader`/`alignmentGuide`/`.coordinateSpace`; `Image("name")` asset catalog; `AsyncImage(url:)`; `.scrollPosition`/`ScrollViewReader` (host-pushed intent form).

---

## 4. Honest hard limits

A tree-walking interpreter over an erased type system reaches the entire *declarative* surface of SwiftUI: any view, modifier, style, layout, control, navigation, and animation that is ultimately a value transform the host can render or re-render. With the four unlocks it can interpret essentially any *idiomatic* SwiftUI sidebar. The boundary is structural, not a matter of effort:

- **No Scene-level surface.** `commands`/`CommandMenu`/`WindowGroup` attach to a Scene the author does not own. Permanently out of scope; route to host settings.
- **No child-to-parent value flow.** `PreferenceKey`/`onPreferenceChange` need a two-pass collect-reduce-deliver with a user `reduce`; the walk is one-pass top-down. Infeasible without a fundamentally different engine.
- **No imperative drawing builders.** `Canvas` and `Path { p in … }` need a stateful mutable builder/context object with a curated imperative method set; custom `struct: Shape` additionally needs protocol conformance plus a render-time rect. These are the union of every hard part and stay out of scope (convenience non-closure shape inits are fine).
- **No true compile-time guarantees.** Types are erased, so there is no type checking, overload resolution by type, exhaustiveness checking, or `KeyPath`/keyframe generality beyond hardcoded tracks. Authors get runtime behavior, not compiler diagnostics.
- **Deferred-layout values are host-borrowed, not interpreted.** `GeometryReader`, `alignmentGuide`, `@FocusState`, and self-updating date/timer text work only because the SwiftUI bridge owns the real proxy/focus/runloop and re-invokes a stored closure or mirrors a cell. The interpreter never *holds* layout geometry, keyboard focus, or a clock; it projects host-supplied values into a re-evaluated closure.
- **No general user-value animation.** `keyframeAnimator`'s general form (user value struct + keypath tracks) and custom alignment IDs need declaration shapes (`extension { static let }`, arbitrary keypaths into user structs) that the language layer cannot synthesize. Constrained named-track subsets are the pragmatic ceiling.

The pragmatic line for "interpret any SwiftUI": **anything expressible as a `View` value (or a transform over one) that the host can render or be asked to re-render is reachable; anything requiring Scene ownership, bottom-up aggregation, a user-defined imperative drawing/animation object, or real compile-time type guarantees is not.**

---

## Completeness Critique & Additions

- **Accessibility (`.accessibilityRepresentation`, `.accessibilityActivationPoint`, richer sort/rotor semantics)** — leaf/runtime bridge. Windows/Tauri now covers the label/value/hint/traits/element-children/action/activation-point/representation/sort-priority metadata core, mapping direct equivalents to ARIA and preserving richer SwiftUI semantics as render breadcrumbs. `accessibilityRepresentation` closure children are preserved as a hidden metadata mirror, while native representation substitution, real activation geometry, real sort behavior, and full VoiceOver/native bridge semantics remain follow-up; keep the remaining a11y work at P1, not P2.

- **`.accessibilityAction(named:)` / `accessibilityAction(.default)`** — stateEngine. Windows/Tauri now supports the constrained safe-action subset: authored `.accessibilityAction { cmux(...) }` / `.accessibilityAction(named:) { cmux(...) }` metadata is preserved as breadcrumbs/classes and invoked from keyboard Enter/Space when attached to a rendered node. Full native custom action menus, arbitrary closure execution, platform assistive-tech routing, and activation-point semantics remain follow-up.

- **`scenePhase` (`@Environment(\.scenePhase)`)** — infeasible (or host-borrowed runtimeLimited at best). The spec covers `@Environment` reads generically (typeSystem, P1/P2) but never calls out scenePhase. It is a Scene-level signal; an embedded sidebar view has no Scene, so active/inactive/background is not author-observable. Belongs explicitly in the infeasible list alongside `commands`/`WindowGroup`, or as a host-pushed read-only allowlisted value if the host chooses to surface it.

- **`@ScaledMetric`** — typeSystem (declaration registry) + runtimeLimited (host owns Dynamic Type scaling). The spec lists `.dynamicTypeSize` (leaf, P2) but not `@ScaledMetric`, which is a property wrapper whose scaled value must be recomputed by the host against the current size category. Needs the property-wrapper var-decl parsing from Phase 3 plus a host-supplied scale factor. Currently unaddressed.

- **Redaction / privacy (`.redacted(reason:)`, `.unredacted`, `.privacySensitive()`, `RedactionReasons`)** — leaf. Windows/Tauri now preserves the constrained leaf intent as placeholder/privacy classes and `data-swift-*` breadcrumbs, including `.unredacted()` override metadata. Inherited environment propagation, system privacy lock-state behavior, and exact SwiftUI placeholder drawing remain broader P1 follow-up.

- **Coordinate spaces (`.coordinateSpace(name:)` + `.named()` lookups)** — leaf + runtimeLimited split. Windows/Tauri now ships the leaf registration half as stable classes/data breadcrumbs. Resolving a frame in a named space is still host-borrowed deferred layout and remains tied to the future `GeometryProxy` seam.

- **Transferable / typed drag-and-drop (`Transferable` conformance, `.dropDestination(for: MyType.self)`, `.draggable` with a Transferable payload, `.itemProvider`)** — infeasible for the *typed/codable* form; runtimeLimited for the *string-payload* form. The spec covers `.draggable(_:)` static string (leaf) and `.dropDestination(for:action:)` (stateEngine, deferred action), but typed `Transferable` requires synthesizing protocol conformance plus `UTType` registration that the erased type system cannot express. This boundary should be stated, not implied.

- **`UTType` / `ContentType` values** — typeSystem at best, more realistically host-allowlisted. Needed by drag-and-drop, file importers, and `.fileImporter`. The spec never mentions content types; they cannot be synthesized from author code and must be a host-provided named allowlist (`.plainText`, `.url`, etc.). Worth an explicit note.

- **`.fileImporter` / `.fileExporter` / `.fileMover`** — infeasible (or host-mediated runtimeLimited). These present system pickers and hand back security-scoped URLs; the sidebar interpreter has no entitlement-bearing surface. Should be named in the infeasible/host-only set rather than silently absent.

- **Localization beyond markdown (`LocalizedStringKey` interpolation with inflection/`^[..]` automatic grammar agreement, `Text("\(count) item", comment:)`, string catalogs, `.environment(\.locale)`)** — leaf for the discriminator, runtimeLimited for actual catalog lookup. The spec handles the markdown/verbatim discriminator (P0/P1) but says nothing about who owns the localization table. The interpreter cannot hold a `.xcstrings` catalog; lookup is host-borrowed. Inflection/grammar agreement is SwiftUI-owned and rides for free once the key is passed as a real `LocalizedStringKey`. State this ownership split.

- **RTL / layout direction (`.environment(\.layoutDirection)`, `.flipsForRightToLeftLayoutDirection`, leading/trailing semantics)** — leaf + host layout. Windows/Tauri now preserves constrained `.environment(\.layoutDirection)` writes, read-only `@Environment(\.layoutDirection)`, and `.flipsForRightToLeftLayoutDirection(...)` breadcrumbs/CSS mirror hints. Full inherited RTL image behavior, native leading/trailing layout negotiation, and discouraging hardcoded `.left`/`.right` alignment remain broader design/system follow-up.

- **Units / measurement & number formatting (`Measurement`, `.formatted(.measurement(...))`, `.number`, `.percent`, `.currency(code:)`, `.list(type:)`, `RelativeDateTimeFormatter`/`.relative`)** — typeSystem (the `FormatStyle` values are tagged values with a `.formatted` method) rendered to a static string (leaf output). The spec lists `.formatted` / `Text(_:format:)` generically (P1) but does not enumerate the format-style surface or note that `Measurement`/units need a tagged `SwiftValue` case. The common money/percent/relative-date styles deserve explicit P1 callouts since sidebars routinely show counts, sizes, and timestamps.

- **`PreferenceKey` — partial host-mediated escape hatch correction.** The spec correctly marks general `PreferenceKey`/`onPreferenceChange` infeasible, but should note the one reachable subset: SwiftUI's *built-in* anchor/preference-backed modifiers that don't require a user `reduce` (e.g. `.navigationTitle` via toolbar, `.matchedGeometryEffect`, `.alignmentGuide`) already work through the host. The hard limit is specifically *author-defined* keys with a custom `reduce`. Tighten the wording so it doesn't read as "no preference mechanism at all."

- **`.containerRelativeFrame(_:)` / container-relative sizing** — leaf. Windows/Tauri now ships the constrained axis-set + optional count/span/spacing/alignment metadata path as classes, breadcrumbs, and CSS sizing hints. Native container proposal math and scroll-target sizing remain follow-up, but this should still be preferred over `GeometryReader` where a bounded sidebar-local sizing hint is enough.

- **`.scrollTargetBehavior` / `.scrollTargetLayout` / `.scrollClipDisabled` / `.scrollBounceBehavior` / `.scrollIndicators(_:)`** — leaf. Windows/Tauri now preserves these constrained token modifiers as stable classes/data breadcrumbs, with safe CSS hints where possible. Native paging/snap targets, bounce physics, scroll-position binding, and exact platform scrollbar behavior remain follow-up.

- **`.onGeometryChange(for:of:action:)` (iOS 18 / macOS 15)** — stateEngine + runtimeLimited. Windows/Tauri now preserves constrained authored presence, requested value type, and transform-closure presence as stable classes/data breadcrumbs. The remaining parity work is the deferred host-supplied value plus the action executor to write the bag; no transform/action is executed yet. This remains the modern, less-fragile alternative to many `GeometryReader` state feeds and should ride the deferred-closure seam. P2.

- **`.visualEffect { content, proxy in ... }`** — runtimeLimited (deferred render-time closure). Windows/Tauri now preserves constrained authored presence as stable classes/data breadcrumbs, but does not execute the closure or provide a real `GeometryProxy`. The remaining parity work is the same deferred-proxy mechanism as `GeometryReader`/runtime alignment behavior, applying a value-transform to `content` using host-supplied geometry. P2.

- **`ScrollView` paging / `TabView(.page)` style** — leaf for the style token; the selection form is correctly stateEngine. Windows/Tauri now renders non-selection `TabView` child pages as a sidebar-local container, preserves `.tabViewStyle(.page)` as classes/data breadcrumbs plus a horizontal scroll-snap hint, and exposes simple `.tabItem { Text/Label }` chrome as an inert tab strip. `TabView(selection:)`, native page indicators, and exact platform paging physics remain follow-up.

- **`Gauge` / `ProgressView(value:total:)`** — leaf for the static/determinate value form, stateEngine only if bound. The spec lists no progress/gauge controls at all. `ProgressView()` (indeterminate) and `ProgressView(value:)` (static Double) are pure leaf nodes SwiftUI animates itself; `Gauge` is leaf with a `StyleValue`-resolved tint. A common sidebar element entirely missing from the matrix; P1.

- **`ControlSize` / `.controlSize(_:)`, `.buttonStyle(_:)`, `.buttonBorderShape`, `.pickerStyle`, `.toggleStyle`, `.textFieldStyle`** — leaf (token) for built-in styles; the *custom* `ButtonStyle`/`ToggleStyle` struct form is typeSystem (needs struct + protocol + the `configuration.label` child) or arguably runtimeLimited. The spec mentions `.labelStyle`/`.menuStyle`/`.listStyle` but is silent on the button/toggle/picker/textfield style modifiers, which are high-frequency. Built-in token forms are P1 leaf; flag the custom-style-struct form as a typeSystem item that needs the `StyleConfiguration` child plumbed through the arbitrary-child seam.

- **`@AppStorage` / `@SceneStorage`** — runtimeLimited (host-owned persistence) layered on the state engine. The spec's `@State` bag is in-memory only. `@AppStorage` is a binding cell whose get/set is backed by host `UserDefaults` (cmux settings); feasible and useful for a persistent sidebar, but needs an explicit host-backed cell distinct from the ephemeral `StateBag`. Currently unaddressed.

- **`mis-tier correction: `withAnimation`/`.transition`** — the spec places these in stateEngine (P0/P1), which is correct, but `.transition(_:)` and `.animation(_:value:)` as *inert modifiers* (no state change yet) are leaf. Windows/Tauri now plumbs that inert modifier metadata as classes/breadcrumbs; making it fire remains stateEngine work.

- **`mis-tier correction: `.tag`/`.id`** — listed as leaf (P0/P2), which is right for parsing, but `.id(_:)` forcing identity-based view replacement only has observable effect once re-walks happen (stateEngine). Windows/Tauri currently preserves `.id(_:)` as a render breadcrumb only, so the replacement semantics remain dependent on the later diff/state engine.

- **`Color(nsColor:)` / `Color(uiColor:)` / `Color.accentColor` / system semantic colors (`.primary`, `Color(.systemGray)`)** — leaf, host-mediated for platform/asset colors. The spec widens `dslColor` palette and asset names but doesn't call out platform color bridges or the full semantic-color set (`.primary`/`.secondary` as colors vs. as hierarchical styles is ambiguous in the current spec). Clarify and enumerate; P0/P1.

- **Composition pattern not unlocked: closure-typed view *parameters* on user structs (`init(@ViewBuilder content: () -> Content)` plus `let content: Content` stored and rendered via `content`)** — typeSystem. Phase 3 unlocks `@ViewBuilder` content and custom Views, but the spec doesn't explicitly state that a stored `Content` generic property rendered as `content` (the canonical container-component pattern, e.g. a custom `Card { ... }`) works. This is the single most common reusable-component idiom for a sidebar library; confirm it falls out of `.closure` capture + erased generics, and call it a first-class Phase 3 deliverable.

- **Composition pattern not unlocked: `ViewModifier` structs + `.modifier(_:)` / `.modifier` extension sugar** — typeSystem. Custom `struct: ViewModifier { func body(content:) }` is the idiomatic way authors package reusable modifier chains. It needs struct + protocol conformance + the `content` child (arbitrary-child seam). The roadmap unlocks the seam and custom structs separately but never states that their composition yields user `ViewModifier`s. Should be an explicit Phase 3 deliverable.
