[package]
name = "cmux-agent-resume-argv"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
path = "src/lib.rs"

# No dependencies: `cmux_ipc::shell_quote` (the intended reuse target for
# `posixSingleQuoted`) is private to `cmux-ipc` and passes safe tokens through
# unquoted, whereas the resume path needs unconditional single-quoting — so the
# two-line helper is re-ported verbatim in `src/resume_argv.rs` instead.
[dependencies]
